import { h } from "preact"
import { css } from "emotion"
import { PRIMARY_COLOR } from "../theme"

const rootCss = css({
  height: "100%",
})

export const GlossaryPage = () => {
  return <div className={rootCss}>Wow</div>
}
